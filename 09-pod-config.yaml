#refering the values from configmap
kind: Pod
apiVersion: v1
metadata:
  name: pod-config
spec:
  containers:
    - name: nginx
      image: nginx
      env:
      - name: course
        ValueFrom:
          configMapKeyRef:
            name: configmap
            key: course
      - name: trainer
        ValueFrom:
          configMapKeyRef:
            name: configmap
            key: trainer
      - name: duration
        ValueFrom:
          configMapKeyRef:
            name: configmap
            key: duration